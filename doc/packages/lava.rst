============
Lava Package
============

The Lava package is the standard package that is loaded by default. It registers the core services, adds in the core middleware, and bootstraps the other packages.
